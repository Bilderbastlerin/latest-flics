# latest-flics

A little coding exercise

## Add your API key

- Open (or create if not present) the file `local.properties` in the root path of this project
- Add the property `API_KEY` and set its value to your personal API key for the TMDB web service. 
- It should should look something like this `API_KEY="1234567890"`