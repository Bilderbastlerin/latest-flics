# latest-flics

A little coding exercise

## Add your API key

- Open (or create if not present) the file `local.properties` in the root path of this project
- Add the property `api_token` and set its value to your personal API token for the TMDB web service. 
- It should should look something like this `api_token="1234567890"`